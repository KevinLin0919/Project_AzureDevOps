steps:
- script: |
    python3 asgards/scripts/benchmark.py asgards/src/main.py -n 5
    if [ -f benchmark_summary.md ]; then
      echo "##vso[task.uploadsummary]$(pwd)/benchmark_summary.md"
    fi
  displayName: 'Performance Benchmarking (CPU, Memory, Time)'
